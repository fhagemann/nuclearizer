name: Nuclearizer Tests for develop/em
on:
  push:
    branches:
      - develop/em
    tags: '*'
  pull_request:
    branches:
      - develop/em
  release:

jobs:
  build:
    name: Ubuntu
    runs-on: ubuntu-latest
    env:
      MEGALIB: ${{ github.workspace }}/megalib
      NUCLEARIZER: ${{ github.workspace }}

      # ccache settings
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CCACHE_COMPRESS: true
      CCACHE_COMPILERCHECK: content
      CCACHE_NOHASHDIR: true
      CCACHE_BASEDIR: ${{ github.workspace }}
      CCACHE_MAXSIZE: 2G
      NUCLEARIZER_CACHE_DIR: ${{ github.workspace }}/.nuclearizer-cache

    steps:
      - name: Setup Conda Environment
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-name: cosi-env
          channels: conda-forge
          create-args: >-
            root=6.32 geant4=11.2 healpix_cxx
            pkg-config make cmake cxx-compiler ccache
            ccfits=2.6 cfitsio=4.3 hdf5=1.14
          cache-environment: true

      - name: Write pkg-config files & sanity-check
        shell: micromamba-shell {0}
        run: |
          set -euo pipefail

          echo -e "prefix=$CONDA_PREFIX\nexec_prefix=\${prefix}\nlibdir=\${exec_prefix}/lib\nincludedir=\${prefix}/include\n\nName: hdf5\nDescription: HDF5\nVersion: 1.14\nLibs: -L\${libdir} -lhdf5 -lhdf5_cpp\nCflags: -I\${includedir}" > "$CONDA_PREFIX/lib/pkgconfig/hdf5.pc"
          echo -e "prefix=$CONDA_PREFIX\nexec_prefix=\${prefix}\nlibdir=\${exec_prefix}/lib\nincludedir=\${prefix}/include\n\nName: CCfits\nDescription: CCfits\nVersion: 2.6\nLibs: -L\${libdir} -lCCfits\nCflags: -I\${includedir}" > "$CONDA_PREFIX/lib/pkgconfig/CCfits.pc"

          export PKG_CONFIG_PATH="$CONDA_PREFIX/lib/pkgconfig:$CONDA_PREFIX/share/pkgconfig:${PKG_CONFIG_PATH:-}"
          pkg-config --print-errors --exists hdf5
          pkg-config --print-errors --exists CCfits

      - name: Checkout Nuclearizer
        uses: actions/checkout@v4

      - name: Checkout MEGAlib
        uses: actions/checkout@v4
        with:
          repository: zoglauer/megalib
          ref: develop-cosi
          path: megalib

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          create-symlink: true
          key: ${{ runner.os }}-ccache-v1          # stable — never thrown away
          restore-keys: ${{ runner.os }}-ccache-   # always restores the latest
          max-size: 2G

      - name: Cache MEGAlib build
        id: cache-megalib
        uses: actions/cache@v4
        with:
          path: |
            ${{ github.workspace }}/megalib/lib
            ${{ github.workspace }}/megalib/bin
            ${{ github.workspace }}/megalib/include
            ${{ github.workspace }}/megalib/config
          key: megalib-${{ runner.os }}-${{ hashFiles('megalib/src/**', 'megalib/configure') }}
          restore-keys: megalib-${{ runner.os }}-

      - name: Build MEGAlib
        if: steps.cache-megalib.outputs.cache-hit != 'true'
        shell: micromamba-shell {0}
        run: |
          cd "$MEGALIB"
          ./configure
          make -j"$(nproc)"

      # ── Nuclearizer overlay cache (changes often, few MB) ───────────────────
      #
      # All nuclearizer outputs land inside $MEGALIB/lib and $MEGALIB/bin, which
      # are already covered by the MEGAlib cache above. To cache only the
      # nuclearizer-specific subset with its own key, we stage those files into
      # .nuclearizer-cache/ before saving and copy them back before building.
      #
      # Files captured:
      #   megalib/lib/MModule*.{o,d}            – nuclearizer object + dep files
      #   megalib/lib/MFretalon*.{o,d}          – fretalon object + dep files
      #   megalib/lib/libNuclearizer.*           – shared lib + rootmap + rdict
      #   megalib/lib/Nuclearizer_Dictionary.*  – ROOT dict source, object, linkdef
      #   megalib/bin/nuclearizer               – the main executable
      #
      - name: Restore Nuclearizer cache
        id: cache-nuclearizer
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.nuclearizer-cache
          key: nuclearizer-${{ runner.os }}-${{ hashFiles('src/**', 'include/**', 'apps/**', 'Makefile') }}
          restore-keys: nuclearizer-${{ runner.os }}-

      - name: Restore Nuclearizer outputs into MEGAlib tree
        if: steps.cache-nuclearizer.outputs.cache-hit == 'true'
        run: |
          set -euo pipefail
          cp -a "$NUCLEARIZER_CACHE_DIR/lib/." "$MEGALIB/lib/"
          cp -a "$NUCLEARIZER_CACHE_DIR/bin/." "$MEGALIB/bin/"

          # Get list of changed source files (only cxx/h files we care about)
          CHANGED_SOURCES=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} -- 'src/*.cxx' 'include/*.h' 'apps/**' 'Makefile' 2>/dev/null || echo "")
          
          if [ -z "$CHANGED_SOURCES" ]; then
            echo "No source changes detected — all cached artifacts are valid"
            exit 0
          fi
          
          echo "Changed files:"
          echo "$CHANGED_SOURCES"
          
          # For each changed source file, delete its corresponding .o and .d files
          # so Make will rebuild them
          while IFS= read -r file; do
            if [[ "$file" == src/*.cxx ]]; then
              basename="${file##*/}"
              objfile="${MEGALIB}/lib/${basename%.cxx}.o"
              depfile="${MEGALIB}/lib/${basename%.cxx}.d"
              echo "Removing cached artifacts for $file"
              rm -f "$objfile" "$depfile"
            elif [[ "$file" == include/*.h ]]; then
              # Header changed — remove all .o/.d files that depend on it
              basename="${file##*/}"
              basename="${basename%.h}"
              objfile="${MEGALIB}/lib/${basename}.o"
              depfile="${MEGALIB}/lib/${basename}.d"
              echo "Removing cached artifacts for $file"
              rm -f "$objfile" "$depfile"
            elif [[ "$file" == "Makefile" || "$file" == apps/* ]]; then
              # Full rebuild needed
              echo "Makefile or apps changed — clearing all nuclearizer artifacts"
              rm -f "$MEGALIB/lib"/{MModule,MFretalon,Nuclearizer_Dictionary}*.{o,d}
              rm -f "$MEGALIB/lib"/libNuclearizer.*
              rm -f "$MEGALIB/bin"/nuclearizer
              break
            fi
          done <<< "$CHANGED_SOURCES"
          
          echo "Cleaned up stale artifacts for changed files"

      - name: Configure build environment
        shell: micromamba-shell {0}
        run: |
          mkdir -p "$CONDA_PREFIX/lib/pkgconfig"
          export PKG_CONFIG_PATH="$CONDA_PREFIX/lib/pkgconfig:$CONDA_PREFIX/share/pkgconfig:${PKG_CONFIG_PATH:-}"
          export LD_LIBRARY_PATH="$MEGALIB/lib:$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export CXX="ccache g++"
          export CC="ccache gcc"

          echo "PKG_CONFIG_PATH=$PKG_CONFIG_PATH" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=$LD_LIBRARY_PATH" >> "$GITHUB_ENV"
          echo "LDFLAGS=-L$CONDA_PREFIX/lib -Wl,-rpath,$CONDA_PREFIX/lib ${LDFLAGS:-}" >> "$GITHUB_ENV"
          echo "CXX=$CXX" >> "$GITHUB_ENV"
          echo "CC=$CC" >> "$GITHUB_ENV"

          ccache --zero-stats || true

      - name: Build Nuclearizer
        shell: micromamba-shell {0}
        run: |
          cd "$NUCLEARIZER"
          make -j"$(nproc)"

      - name: Stage Nuclearizer outputs for caching
        run: |
          set -euo pipefail
          CACHE="$NUCLEARIZER_CACHE_DIR"
          LB="$MEGALIB/lib"
          BN="$MEGALIB/bin"

          mkdir -p "$CACHE/lib" "$CACHE/bin"

          # Capture ALL .o, .d, and related files that nuclearizer's make wrote
          # into megalib/lib — identified by type, not by filename prefix.
          # This is robust regardless of how many or what files nuclearizer adds.
          find "$LB" -maxdepth 1 \( \
            -name '*.o' -o -name '*.d' \
          \) -newer "$MEGALIB/config/Makefile.config" \
            -exec cp -a {} "$CACHE/lib/" \;

          # Shared lib, rootmap, rdict PCM, dictionary files
          find "$LB" -maxdepth 1 \( \
            -name 'libNuclearizer.*' \
            -o -name 'Nuclearizer_Dictionary*' \
          \) -exec cp -a {} "$CACHE/lib/" \;

          # The binary
          [ -f "$BN/nuclearizer" ] && cp -a "$BN/nuclearizer" "$CACHE/bin/"

          echo "Staged $(find "$CACHE" -type f | wc -l) files into nuclearizer cache:"
          find "$CACHE" -type f | sort

      - name: Test Nuclearizer
        shell: micromamba-shell {0}
        run: |
          cd "$NUCLEARIZER"
          make test -j"$(nproc)"

      - name: Upload Logs on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: failure-logs
          path: ${{ github.workspace }}/*.log